{
  "name": "social-media-mcp",
  "version": "2.0.0",
  "description": "Tenovio LinkedIn Marketing Agent",
  "type": "module",
  "scripts": {
    "start": "node post-now.mjs",
    "agent": "node agent-start.mjs",
    "dashboard": "node src/approvalServer.mjs",
    "scheduler": "node src/scheduler.mjs",
    "generate": "node -e \"import('./src/agent.mjs').then(m => m.generatePost(0).then(p => m.savePendingPost(p)).then(() => { console.log('Post generated! Open http://localhost:3000'); process.exit(0); }))\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.6.1"
  }
}
